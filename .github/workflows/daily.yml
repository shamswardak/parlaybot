name: Daily parlays

on:
  schedule:
    - cron: "0 16 * * *"
  workflow_dispatch:
    inputs:
      ticket:
        description: "Which ticket to build"
        type: choice
        default: "All three"
        options: ["All three", "Safe 20", "Core 10", "Trend 5"]
      sport:
        description: "Sport"
        type: choice
        default: "All"
        options: ["All", "MLB", "NFL", "NBA", "NHL"]
      date:
        description: "Slate date YYYY-MM-DD (blank = today ET)"
        required: false
      dry_run:
        description: "Print only, do not post to Discord"
        type: boolean
        default: false
      skip_grade:
        description: "Skip grading"
        type: boolean
        default: true

concurrency:
  group: daily-parlays
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -r requirements.txt

      - name: Restore data cache
        uses: actions/cache@v4
        with:
          path: .cache
          key: parlaybot-cache-${{ github.run_id }}
          restore-keys: |
            parlaybot-cache-

      - name: Grade previous slates
        if: ${{ github.event_name == 'schedule' || github.event.inputs.skip_grade != 'true' }}
        continue-on-error: true
        env:
          DISCORD_RESULTS_WEBHOOK_URL: ${{ secrets.DISCORD_RESULTS_WEBHOOK_URL }}
          TZ: America/New_York
        run: python -m parlaybot.grade_main

      - name: Build and send
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_ONDEMAND_WEBHOOK_URL: ${{ secrets.DISCORD_ONDEMAND_WEBHOOK_URL }}
          TZ: America/New_York
        run: |
          ARGS=""
          TICKET="${{ github.event.inputs.ticket }}"
          SPORT="${{ github.event.inputs.sport }}"

          # A manual run with a specific ticket or sport is an ad-hoc build:
          # its own channel, its own history file, so it can't overwrite the
          # scheduled ticket the grader is waiting on.
          if [ -n "$TICKET" ] && [ "$TICKET" != "All three" ]; then
            ARGS="$ARGS --ticket \"$TICKET\" --on-demand"
          elif [ -n "$SPORT" ] && [ "$SPORT" != "All" ]; then
            ARGS="$ARGS --on-demand"
          fi
          if [ -n "$SPORT" ] && [ "$SPORT" != "All" ]; then
            ARGS="$ARGS --sports $SPORT"
          fi
          if [ -n "${{ github.event.inputs.date }}" ]; then
            ARGS="$ARGS --date ${{ github.event.inputs.date }}"
          fi
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            ARGS="$ARGS --dry-run"
          fi
          eval python -m parlaybot.main $ARGS

      - name: Commit history
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          git config user.name "parlaybot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add history/
          if git diff --staged --quiet; then
            echo "nothing new to record"
          else
            git commit -m "history: $(date -u +%Y-%m-%dT%H:%M)"
            git pull --rebase --autostash origin ${{ github.ref_name }} || true
            git push
          fi

      - name: Upload ticket JSON
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: parlays
          path: output/*.json
          if-no-files-found: ignore
          retention-days: 30
